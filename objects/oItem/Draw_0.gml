/// @description Insert description here
// You can write your code in this editor
draw_sprite(sprite, -1, x, y);

if(IsOnFire == Degree.full || IsOnFire == Degree.half)
{
	draw_sprite(sFire, -2, x, y);
}